# Backend (FastAPI + LangGraph) runtime dependencies.
# Installed into the repo-root .venv. Version floors match what the app was
# developed/verified against.

# ── web / api ──
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.20
httpx>=0.27.0

# ── data / config ──
pydantic>=2.10.0
pydantic-settings>=2.5.0

# ── orchestration ──
langgraph>=0.2.70
langgraph-checkpoint-postgres>=3.0.0

# ── database (Neon Postgres) ──
SQLAlchemy>=2.0.40
asyncpg>=0.30.0
psycopg[binary]>=3.2.0
alembic>=1.13.0

# ── LLM + embeddings (Azure OpenAI) + Document Intelligence ──
openai>=1.40.0
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0

# ── vector store ──
qdrant-client>=1.12.0

# ── object storage (Cloudflare R2 via S3 API) ──
boto3>=1.34.0

# ── document processing ──
python-docx>=1.1.0
docxtpl>=0.16.0
Jinja2>=3.1.0
pdfplumber>=0.11.0
PyMuPDF>=1.25.0
Pillow>=11.0.0

# ── retrieval ──
rank-bm25>=0.2.2
numpy>=1.26.0

# ── compliance report charts ──
matplotlib>=3.8.0
